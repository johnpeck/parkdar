/* pd_ranger.h
 * 
 * Functions for dealing with range finding */

/* Calibration structure */
struct cal_struct {
    int slope; // Measurements are multiplied by this factor before offset
    int offset; // Measurements are offset by this factor after slope is applied
};


/* This is the junk function.
 * It's great */
void junkfunc(void);


/* This is the crap function.
 * It's also great */
void crapfunc(void);
